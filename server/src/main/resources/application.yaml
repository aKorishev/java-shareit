spring:
  jpa:
    hibernate.ddl-auto: none
    properties:
      hibernate:
        jdbc.time_zone: UTC
        format_sql: true
  sql.init:
    mode: always
    schema-locations: classpath:schema.sql
logging.level.org.springframework: INFO